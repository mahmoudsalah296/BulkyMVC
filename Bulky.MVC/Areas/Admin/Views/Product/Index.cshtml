@model List<Product>

<div class="card shadow my-4 border-0">
    <div class="card-header bg-secondary bg-gradient ml-0 py-3">
        <div class="row">
            <div class="col-12 text-center">
                <h2 class="text-white py-2">Product List</h2>
            </div>
        </div>
    </div>
    <div class="card-body p-4">
        <div class="row pb-3">
            <div class="col-6"></div>
            <div class="col-6 text-end">
                <a class="btn btn-primary" asp-action="Upsert">
                    <i class="bi bi-plus-square text-warning pe-2"></i>
                    Add new product
                </a>
            </div>
        </div>
        <table id="tableData" class="table table-bordered table-striped border-opacity-10 border-black ">
            <thead>
            <tr>
                <th>Book Title</th>
                <th>Book Author</th>
                <th>Book ISBN</th>
                <th>Price</th>
                <th>Category</th>
                <th></th>
            </tr>
            </thead>

            @* <tbody>
            @foreach (Product product in Model)
            {
                <tr>
                    <td>@product.Title</td>
                    <td>@product.Author</td>
                    <td>@product.ISBN</td>
                    <td>@product.Price</td>
                    <td>@product.Category.Name</td>
                    <td>
                        <div class="w-75 btn-group" role="group">
                            <a asp-action="Upsert" class="btn btn-primary mx-2" asp-route-id="@product.Id">
                                <i class="bi bi-pencil-square"></i>
                                Edit
                            </a>
                            <a asp-action="Delete" class="btn btn-danger mx-2" asp-route-id="@product.Id">
                                <i class="bi bi-trash-fill"></i>
                                Delete
                            </a>
                        </div>
                    </td>
                </tr>
            }
            </tbody> *@
        </table>
    </div>
</div>

@section Scripts
{
    <script src="~/js/product.js"></script>
}
